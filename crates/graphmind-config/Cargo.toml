[package]
name = "graphmind-config"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Shared configuration, paths, and project registry for GraphMind"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
dirs = "5"
chrono = "0.4"

[dev-dependencies]
tempfile = "3"
